# Minecraft Manager - Environment Variables Configuration
# Copy this file to .env and configure your values

# ==============================================================================
# DOMAIN CONFIGURATION
# ==============================================================================

# Base domain for your Minecraft servers (without subdomain)
# Minecraft servers will be accessible at: <subdomain>.<MC_SUBDOMAIN_PREFIX>.<BASE_DOMAIN>
# Example: if BASE_DOMAIN=example.com and MC_SUBDOMAIN_PREFIX=mc, servers will be at server1.mc.example.com
BASE_DOMAIN=example.com

# Subdomain prefix for Minecraft servers
# This is the prefix used between server name and base domain
# Example: if MC_SUBDOMAIN_PREFIX=ry, servers will be at server1.ry.example.com
MC_SUBDOMAIN_PREFIX=mc

# Manager web interface domain
# This is where you'll access the Minecraft Manager dashboard
MANAGER_DOMAIN=manager.example.com

# Traefik dashboard domain (optional, for monitoring)
TRAEFIK_DOMAIN=traefik.example.com

# ==============================================================================
# SSL/TLS CONFIGURATION
# ==============================================================================

# Email for Let's Encrypt certificate notifications
# You'll receive expiration warnings and renewal notices here
ACME_EMAIL=admin@example.com

# ==============================================================================
# MC-ROUTER CONFIGURATION
# ==============================================================================

# Initial mc-router mapping (optional)
# This will be automatically managed by the application
# Format: domain1=container1:port,domain2=container2:port
MC_ROUTER_MAPPING=

# ==============================================================================
# EXAMPLE CONFIGURATION
# ==============================================================================

# For domain: mydomain.com with prefix "mc"
# BASE_DOMAIN=mydomain.com
# MC_SUBDOMAIN_PREFIX=mc
# MANAGER_DOMAIN=manager.mydomain.com
# TRAEFIK_DOMAIN=traefik.mydomain.com
# ACME_EMAIL=admin@mydomain.com
#
# With this configuration:
# - Minecraft Manager UI: https://manager.mydomain.com
# - Traefik Dashboard: http://traefik.mydomain.com:8080
# - Minecraft Server 1: survival.mc.mydomain.com:25565
# - Minecraft Server 2: creative.mc.mydomain.com:25565
#
# For a different prefix (e.g., "ry"):
# MC_SUBDOMAIN_PREFIX=ry
# - Minecraft Server 1: survival.ry.mydomain.com:25565

# ==============================================================================
# IMPORTANT DNS CONFIGURATION
# ==============================================================================

# You MUST configure these DNS records:
# 1. A record: manager.example.com → Your server IP
# 2. A record: traefik.example.com → Your server IP (optional)
# 3. A record: *.<MC_SUBDOMAIN_PREFIX>.example.com → Your server IP (wildcard for all MC servers)
#
# Example DNS records for mydomain.com at IP 1.2.3.4 with MC_SUBDOMAIN_PREFIX=mc:
# manager.mydomain.com    A    1.2.3.4
# traefik.mydomain.com    A    1.2.3.4
# *.mc.mydomain.com       A    1.2.3.4
#
# Example DNS records with MC_SUBDOMAIN_PREFIX=ry:
# managerry.mydomain.com  A    1.2.3.4
# *.ry.mydomain.com       A    1.2.3.4
